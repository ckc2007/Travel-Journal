<div class="">
  <h2 class="text-3xl font-bold pt-10 ml-8 text-black">{{name}}</h2>
  <div class="h-1 bg-gray-300 w-55 mt-2 pl-4 ml-8"></div>

  <p
    class="text-lg text-black-500 font-normal my-4 leading-loose text-center pl-10 pr-10 pt-5 pb-1"
  >{{blog}}</p>

  <p class="text-md font-light text-center pb-5">Story by
    {{user.name}}
    on
    {{format_date date_created}}</p>

  {{#if images}}
    <div class="grid grid-cols-3 gap-4 mt-8 mx-8">
      {{#each images}}
        <a href="/images/{{this}}" target="_blank">
          <div class="image-container">
            <img
              src="/images/{{this}}"
              alt="Story Image"
              class="gallery-image mx-auto h-200 w-200 object-cover rounded hover:outline-blue-500 hover:transform hover:scale-105 transition ease-in-out duration-300"
            />
          </div>
        </a>
      {{/each}}
    </div>
  {{/if}}
</div>

<div class="comment-container">
  <div class="w-full bg-white rounded-lg border p-2 my-4 mx-6 shadow-md">
    <h3 class="font-bold">Discussion</h3>
    <form>
      <div class="w-full px-3 my-2 justify-between flex">
        <textarea
          class="bg-white rounded leading-normal resize-none w-full h-12 py-2 px-3 font-medium placeholder-gray-700 focus:outline-none focus:bg-white border-b-2"
          id="comment-textarea"
          name="body"
          placeholder="Comment Here"
          required
        ></textarea>
        <div class="flex justify-end px-1">
          <input
            type="submit"
            class="px-1 py-1 rounded-md text-white text-sm bg-black ml-2 hover:scale-105 hover:cursor-pointer"
            id="comment-submit"
            value="Post Comment"
          />
        </div>
      </div>
      {{#each comments as |comment|}}
        <div class="flex flex-col">
          <div class="rounded-md p-3 ml-3 my-3">
            <div class="flex gap-3 items-center">
              <img src="/imgs/coffee.svg" class="object-cover w-8 h-8" />
              <h3 class="font-bold">{{comment.user.name}}</h3>
            </div>
            <p class="text-gray-600 mt-2">{{comment.comments}}</p>
            {{#if comment.id}}
              <button
                data-id="{{comment.id}}"
                class="text-gray-400 mt-2 text-sm px-2 py-1 rounded-md"
                id="delcomment"
              >delete</button>
            {{else}}
              <div></div>
            {{/if}}
            <div class="border-b bg-gray-300 w-25 mt-2"></div>
          </div>
        </div>
      {{/each}}
    </form>
  </div>
</div>

<link rel="stylesheet" href="/css/comments.css" />
<link rel="stylesheet" href="/css/index.css" />
<script src="/js/story.js"></script>







<div>
  <section class="flex slideshow justify-center items-center px-6 mt-4">
   
   
    <!-- category card -->
    <div class="card relative">
      <img src="/imgs/beach-seats.jpg" alt="" />
      <div class="absolute overlay-text w-full flex flex-col items-center">
        <p class="text-white text-xl tracking-tight">Waterfall</p>
        <p class="text-xs text-gray-500 font-semibold uppercase mt-1">
          43 Adventures
        </p>
      </div>
    </div>
    
<!-- category card -->
    <div class="card relative">
      <img src="/imgs/beach-boy.jpg" alt="" />
      <div class="absolute overlay-text w-full flex flex-col items-center">
        <p class="text-white text-xl tracking-tight">Lake</p>
        <p class="text-xs text-gray-500 font-semibold uppercase mt-1">
          43 Adventures
        </p>
      </div>
    </div>

  </section>
  <section class="mt-16">
        <!-- slide indicators -->
        <div class="flex mx-auto justify-center">
          <div class="h-1 w-10 bg-gray-400 mr-4"></div>
          <div class="h-1 w-10 bg-gray-400 mr-4"></div>
          <div class="h-1 w-10 bg-blue-600 mr-4"></div>
          <div class="h-1 w-10 bg-gray-400 mr-4"></div>
          <div class="h-1 w-10 bg-gray-400"></div>
        </div>
  </section>
</div>

<script src="/js/glide.js"></script>